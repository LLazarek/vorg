

- Should verify that writes from the UI only ever add property lines
- Can we have a file-level property with a file ID that serves as the stable connection between the org file and the sidecar? That way don't rely on file name, and also makes it easy to recover the sidecar if I move a note file and forget to move the sidecar alongside -- can grep for the ID!
